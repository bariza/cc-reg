//import { SelfRegisterService } from './self.register.service';
import { Location } from '@angular/common';

//describe('SelfRegisterSerive', () => {
 // let selfRegService: SelfRegisterService,
 //   location: Location,
 //   mockHttp;

 // beforeEach(() => {
 //   mockHttp = jasmine.createSpyObj('mockHttp', ['get', 'post']);
 //   selfRegService = new SelfRegisterService(mockHttp, location);
 // });

  // describe('submit step 1', () => {
  //   it('should call http.post with right url', () => {
  //     mockHttp.post.and.returnValue(Observable.of(false));
  //
  //     selfRegService.submitStep1({}, {});
  //
  //     expect(mockHttp.post).toHaveBeenCalledWith('https://bolbbccld1web01.dev.bom.com/api-selfreg/rest/v1.0/step1', jasmine.any(Object));
  //   });
  // });
  //
  // describe('submit step 2', () => {
  //   it('should call http.post with right url', () => {
  //     mockHttp.post.and.returnValue(Observable.of(false));
  //
  //     selfRegService.submitStep2({}, {});
  //
  //     expect(mockHttp.post).toHaveBeenCalledWith('https://bolbbccld1web01.dev.bom.com/api-selfreg/rest/v1.0/step2', jasmine.any(Object));
  //   });
  // });

//});

